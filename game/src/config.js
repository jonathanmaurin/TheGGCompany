export const GAME_CONFIG = {
  INITIAL_SPEED: 200,       // px/sec
  MAX_SPEED: 600,           // px/sec
  SPEED_INCREMENT: 30,      // px/sec per 500m tier
  JUMP_VELOCITY: 520,       // px/sec upward
  OBSTACLE_INITIAL_DELAY: 1500, // ms before first obstacle
  COIN_INITIAL_DELAY: 2000,     // ms before first coin
};
